# Backend Vendor Packages

This directory contains small shared packages that ChatRPG imports at runtime
and must ship with the open-source repository and Docker image.

`trpg_module_parser_core` was originally shared from a sibling development
checkout. Keeping a copy here makes fresh clones and server deployments
self-contained.
